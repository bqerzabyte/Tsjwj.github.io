// دالة لفك JWT من Googlefunction parseJwt(token) {    const base64Url = token.split('.')[1];    const base64 = base64Url.replace(/-/g, '+').replace(/_/g, '/');    return JSON.parse(atob(base64));}// دالة إرسال البريد عند تسجيل الدخولfunction sendLoginEmail(name, email) {    emailjs.send("service_zgep8hh", "template_17wjfzx", {        user_name: name,        user_email: email,        message: `قام ${name} (${email}) بتسجيل الدخول على موقعك.`    }, "28feFNTo2jE91PZYP")    .then(function(response) {        console.log("تم إرسال التنبيه بنجاح!", response.status, response.text);    }, function(error) {        console.error("فشل إرسال البريد:", error);    });}